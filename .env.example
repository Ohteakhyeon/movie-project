# WAS tier → DB tier connection
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=movie_user
DB_PASSWORD=movie_pass
DB_NAME=movie_db

# WAS tier server
WAS_PORT=3001
CORS_ORIGIN=http://localhost:5173

# Web tier (production build — dev uses Vite proxy)
VITE_API_BASE_URL=
